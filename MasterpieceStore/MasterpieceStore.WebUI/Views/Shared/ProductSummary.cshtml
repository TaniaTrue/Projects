@model MasterpieceStore.Domain.Entities.Product

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span>@Model.MinPrice.ToString("c")</span>
    </h3>
    <span class="lead"> @Model.Description</span>
</div>